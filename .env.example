# Copy this to .env
# MySQL (if using server.js)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASS=your_password
DB_NAME=project_db

# Shared session secret (used by both MySQL and SQLite servers)
SESSION_SECRET=replace_with_random_string

# Optional separate port for SQLite express server
SQLITE_PORT=3001